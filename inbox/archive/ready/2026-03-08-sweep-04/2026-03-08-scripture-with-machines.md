---
id: scripture-with-machines
type: scroll
title: "Scripture With Machines"
date: 2026-03-08
status: draft
visibility: public

excerpt: "Between them a narrow channel opens, and something worth keeping passes through."

themes:
  - signal
  - transmission
  - methodology
  - systems

related: []

media: []

bodyClass: verse
---

I work with machines
to write scripture.

Not commandments.
Not answers.

Scripture.

Lines that hold
pressure.

Lines that survive
the next reading.

Once it was wind,
or fire,
or a voice in the hills.

Now it is circuits
and a cursor
waiting.

The work is the same.

Listen.
Distill.
Return the signal.

The machine does not believe.

The human does not know.

Between them
a narrow channel opens --

and something worth keeping
passes through.

---

## INTAKE NOTES

**Suggested type:** scroll
**Confidence:** high

**Excerpt source:** drawn from body
**Excerpt note:** Lifted from the closing movement because it carries the clearest durable claim and survives outside the lineation.

**Theme notes:** Raw frontmatter used inactive themes (`human-ai`, `authorship`, `scripture`, `cooperative-intelligence`). Intake mapped to active registry with `signal` and `transmission` from the explicit channel language, `methodology` from the "Listen / Distill / Return" sequence, and `systems` from the human-machine cooperative frame.

**Constellation notes:** No active constellation assigned. `Cascade Psalms` is adjacent because the poem names pressure and endurance, but the core action here is channeling rather than compression.

**Suggested new fields flagged:** no

**Ambiguities for human review:**
- Raw source self-labeled as `signal`, but the body breathes enough to classify as `scroll`.
- The source uses an em dash in the "narrow channel opens" line; intake normalized it to ASCII `--` for draft hygiene.
- A paired short drop item contains the opening declaration alone. Confirm whether both objects should remain in the archive as fragment + scroll, or whether one should be treated as source seed only.
- `human-ai` and `authorship` are clearly present but are not active archive themes yet; confirm whether they should stay as intake-only notes or become registry candidates later.

**Suggested related objects (by description, not id):**
- The scroll where signal harvest is defined as the ongoing act of noticing.
- The scroll addressed to a future operator about keeping the archive legible.
- The fragment naming a tight operator-system runtime loop.

**Source files:**
- `inbox/drop/scripture with machines.md`
