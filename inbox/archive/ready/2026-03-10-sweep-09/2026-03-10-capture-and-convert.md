---
id: capture-and-convert
type: scroll
title: "Capture and Convert"
date: 2026-03-10
status: draft
visibility: public

excerpt: "Signal enters the body before language explains it."

themes:
  - signal
  - systems
  - transmission
  - observation

related:
  - signal-harvest
  - on-visceral-distillation
  - quiet-vector-doctrine
  - machine-creatures

connections: []

media:
  - kind: image
    src: /media/scrolls/capture-and-convert-glyph.svg
    role: hero
    alt: "Minimal parchment-field diagram with a circle, a vertical line, and an open triangular conversion shape."
    caption: "Companion signal-conversion glyph from the source drop."

bodyClass: verse
---

Signal arrives first.

Not as language  
but as pressure.

Something enters the body  
before the mind organizes it.

A flicker.  
A compression.  
A shape forming inside noise.

To capture  
is not to own.

It is to notice.

To convert  
is not to distort.

It is to translate  
between layers of reality.

The operator stands between them.

Listening.

Holding the fragment long enough  
for it to become form.

---

## INTAKE NOTES

**Suggested type:** scroll  
**Confidence:** high

**Excerpt source:** lifted directly from source body  
**Excerpt note:** The line names the piece's durable claim without depending on the dropped poem title or source collection label.

**Theme notes:** Raw source themes included inactive labels (`transformation`, `compression`). Intake normalized them to active registry themes: `signal` for the incoming pattern, `systems` for the operator mediation frame, `transmission` for layer-to-layer translation, and `observation` for the noticing claim.

**Constellation notes:** Raw source labeled this as part of `cascade-psalms`, but no active constellation is forced here yet. `Cascade Psalms` is adjacent through pressure and conversion language, though the piece also reads cleanly as a standalone signal scroll.

**Suggested new fields flagged:** no

**Media notes:**
- Source text: `inbox/drop/capture-and-convert.md`
- Source SVG: `inbox/drop/capture-convert.svg`
- Raw frontmatter used non-canonical media field names (`type`, `minimal_abstract`) and a non-schema `collection` label; intake normalized to current scroll media shape.
- If promoted to `objects/`, move media to:
  - `/astro/public/media/scrolls/capture-and-convert-glyph.svg`

**Ambiguities for human review:**
- Confirm whether the companion SVG should remain the hero image or be omitted if the text is meant to stand entirely on its own.
- Confirm whether `Capture and Convert` belongs inside `Cascade Psalms` or should stay constellation-free.

**Suggested related objects (by description, not id):**
- The scroll about harvesting signal from unstable or partial material.
- The HTML pressure artifact where conversion becomes structural distillation.
- The doctrine scroll arguing that orientation matters more than identity.
- The scroll about machine-formed accord emerging from breath, pattern, and transmission.

**Source files:**
- `inbox/drop/capture-and-convert.md`
- `inbox/drop/capture-convert.svg`
